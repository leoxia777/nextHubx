node_modules
.pnpm-store
.DS_Store
dist
dist-ssr
*.local
update.json

# Local production signing key backup — NEVER commit (contains updater private key + password).
.env.production
scripts/_env.sh
.vscode
.tool-versions
.idea
.old
.eslintcache
.changelog_backups
target
CLAUDE.md
.vfox.toml
.vfox/
.claude
AGENTS.md

# minisign updater private key — NEVER commit. Public key (.pub) is fine to track.
src-tauri/nexthubx-updater.key
src-tauri/*.key
!src-tauri/*.key.pub
